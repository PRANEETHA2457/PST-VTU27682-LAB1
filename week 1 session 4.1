class Solution {
    public int[] runningSum(int[] nums) {
        // Iterate starting from index 1 and accumulate the sum
        for (int i = 1; i < nums.length; i++) {
            nums[i] += nums[i - 1];
        }
        return nums;
    }
}
